# ข้อ 1. ความสัมพันธ์ระหว่างจำนวนชั่วโมงการนอนกับคะแนนสอบ
# นักวิจัยต้องการทราบว่าจำนวนชั่วโมงที่นักเรียนได้นอนหลับต่อคืนมีผลต่อคะแนนสอบของพวกเขาหรือไม่ 
# โดยทำการเก็บข้อมูลจากนักเรียน 10 คนที่มีชั่วโมงการนอนต่างกัน และบันทึกคะแนนสอบของพวกเขา

# ข้อมูลที่ได้จากการเก็บรวบรวมมีดังนี้
# Sleep Hours (ชั่วโมง)
# 4, 5, 6, 7, 4.5, 6.5, 7.5, 8, 5.5, 9  
# Exam Score (คะแนน)
# 55, 60, 65, 70, 58, 68, 75, 80, 63, 85

# สร้างชุดข้อมูล ในรูปแบบของ X (ตัวแปรอิสระ) และ Y (ตัวแปรตาม)
# ใช้ LinearRegression() จาก sklearn.linear_model เพื่อสร้างโมเดล
# Train โมเดล ด้วย model.fit(X, Y)
# คำนวณค่าคาดการณ์ ด้วย model.predict(X)
# แสดงสมการเส้นถดถอย โดยดึงค่า Slope และ Intercept
# คำนวณค่าความแม่นยำของโมเดล โดยใช้ Mean Squared Error (MSE) และ R² (R-squared)
# วาดกราฟ แสดงจุดข้อมูลจริง และเส้นสมการถดถอย



# ข้อ 2. ความสัมพันธ์ระหว่างปริมาณน้ำที่ดื่มกับระดับพลังงาน
# นักโภชนาการต้องการทราบว่าปริมาณน้ำที่ดื่มในแต่ละวัน
# ส่งผลต่อระดับพลังงานที่ได้รับหรือไม่ โดยเก็บข้อมูลจากกลุ่มทดลอง

# ข้อมูลที่ได้จากการเก็บรวบรวมมีดังนี้
# Water Intake (ลิตร)
# 1.5, 2.0, 2.5, 3.0, 1.8, 2.8, 3.2, 3.5, 2.2, 4.0  
# Energy Level (คะแนน)
# 50, 55, 60, 65, 52, 63, 68, 72, 57, 78

# สร้างชุดข้อมูล ในรูปแบบของ X (ตัวแปรอิสระ) และ Y (ตัวแปรตาม)
# ใช้ LinearRegression() จาก sklearn.linear_model เพื่อสร้างโมเดล
# Train โมเดล ด้วย model.fit(X, Y)
# คำนวณค่าคาดการณ์ ด้วย model.predict(X)
# แสดงสมการเส้นถดถอย โดยดึงค่า Slope และ Intercept
# คำนวณค่าความแม่นยำของโมเดล โดยใช้ Mean Squared Error (MSE) และ R² (R-squared)
# วาดกราฟ แสดงจุดข้อมูลจริง และเส้นสมการถดถอย


# ข้อ 3. ความสัมพันธ์ระหว่างเวลาการฝึกซ้อมกับทักษะการเล่นเปียโน
# อาจารย์สอนเปียโนต้องการทราบว่าเวลาการฝึกซ้อมของนักเรียนมีผลต่อทักษะการเล่นเปียโนหรือไม่ 
# โดยได้เก็บข้อมูลของนักเรียน 10 คน
# ที่ฝึกซ้อมเป็นระยะเวลาต่างกัน และให้คะแนนความสามารถของพวกเขา

# ข้อมูลที่ได้จากการเก็บรวบรวมมีดังนี้
# Practice Time (ชั่วโมงต่อสัปดาห์)
# 2, 3, 4, 5, 2.5, 4.5, 5.5, 6, 3.5, 7 
# Skill Level (คะแนนจาก 100)
# 50, 55, 60, 65, 53, 63, 70, 75, 58, 80

# สร้างชุดข้อมูล ในรูปแบบของ X (ตัวแปรอิสระ) และ Y (ตัวแปรตาม)
# ใช้ LinearRegression() จาก sklearn.linear_model เพื่อสร้างโมเดล
# Train โมเดล ด้วย model.fit(X, Y)
# คำนวณค่าคาดการณ์ ด้วย model.predict(X)
# แสดงสมการเส้นถดถอย โดยดึงค่า Slope และ Intercept
# คำนวณค่าความแม่นยำของโมเดล โดยใช้ Mean Squared Error (MSE) และ R² (R-squared)
# วาดกราฟ แสดงจุดข้อมูลจริง และเส้นสมการถดถอย


# ข้อ 4 ศึกษาความสัมพันธ์ระหว่าง "ระยะเวลาการนอนหลับ (Sleep Duration)" กับ "ระดับความเครียด (Stress Level)"
# สถานการณ์นักวิจัยต้องการศึกษาว่าระยะเวลาการนอนหลับมีผลต่อระดับความเครียดของบุคคลหรือไม่ 
# พวกเขาได้ทำการสำรวจคนจำนวนหนึ่งเกี่ยวกับระยะเวลาการนอนหลับของพวกเขาในแต่ละคืน (Sleep Duration) 
# และระดับความเครียดที่วัดได้จากแบบสอบถาม (Stress Level) โดยมีค่าตั้งแต่ 0 (ไม่มีความเครียด) ถึง 100 (เครียดสูงสุด)
# นักวิจัยต้องการใช้ Simple Linear Regression เพื่อทดสอบความสัมพันธ์นี้ ข้อมูลจากไฟล์ sleep_vs_stress.csv

# สร้างชุดข้อมูล ในรูปแบบของ X (ตัวแปรอิสระ) และ Y (ตัวแปรตาม)
# ใช้ LinearRegression() จาก sklearn.linear_model เพื่อสร้างโมเดล
# Train โมเดล ด้วย model.fit(X, Y)
# คำนวณค่าคาดการณ์ ด้วย model.predict(X)
# แสดงสมการเส้นถดถอย โดยดึงค่า Slope และ Intercept
# คำนวณค่าความแม่นยำของโมเดล โดยใช้ Mean Squared Error (MSE) และ R² (R-squared)
# วาดกราฟ แสดงจุดข้อมูลจริง และเส้นสมการถดถอย


# ข้อ 5 ศึกษาความสัมพันธ์ระหว่าง "ระยะเวลาการใช้โทรศัพท์มือถือ (Screen Time)" กับ "คุณภาพการนอนหลับ (Sleep Quality)"
# นักวิจัยด้านสุขภาพต้องการศึกษาว่า "ระยะเวลาการใช้โทรศัพท์มือถือ" ในแต่ละวันส่งผลกระทบต่อ "คุณภาพการนอนหลับ" หรือไม่ 
# โดยทำการสำรวจพฤติกรรมการใช้โทรศัพท์มือถือ (Screen Time) 
# ของกลุ่มตัวอย่าง และวัดคุณภาพการนอนหลับ (Sleep Quality) ด้วยแบบสอบถาม
# Screen Time วัดเป็นจำนวนชั่วโมงที่ใช้โทรศัพท์มือถือในแต่ละวัน
# Sleep Quality วัดเป็นคะแนนคุณภาพการนอน ตั้งแต่ 0 (คุณภาพแย่ที่สุด) ถึง 100 (คุณภาพดีที่สุด)
# นักวิจัยต้องการใช้ Simple Linear Regression เพื่อทดสอบความสัมพันธ์นี้ ข้อมูลจากไฟล์ screen_time_vs_sleep_quality.csv

# สร้างชุดข้อมูล ในรูปแบบของ X (ตัวแปรอิสระ) และ Y (ตัวแปรตาม)
# ใช้ LinearRegression() จาก sklearn.linear_model เพื่อสร้างโมเดล
# Train โมเดล ด้วย model.fit(X, Y)
# คำนวณค่าคาดการณ์ ด้วย model.predict(X)
# แสดงสมการเส้นถดถอย โดยดึงค่า Slope และ Intercept
# คำนวณค่าความแม่นยำของโมเดล โดยใช้ Mean Squared Error (MSE) และ R² (R-squared)
# วาดกราฟ แสดงจุดข้อมูลจริง และเส้นสมการถดถอย


# ข้อ 6 ศึกษาความสัมพันธ์ระหว่าง "ระยะเวลาการฟังเพลง (Listening Time)" 
# กับ "ระดับความผ่อนคลาย (Relaxation Level)"
# นักวิจัยด้านจิตวิทยาต้องการศึกษาว่า "ระยะเวลาการฟังเพลง" 
# ในแต่ละวันส่งผลกระทบต่อ "ระดับความผ่อนคลาย" หรือไม่ โดยทำการสำรวจผู้เข้าร่วมที่ฟังเพลงเป็นประจำ 
# และให้พวกเขาทดสอบระดับความผ่อนคลายหลังการฟังเพลง
# Listening Time วัดเป็นจำนวนชั่วโมงที่ใช้ในการฟังเพลงต่อวัน
# Relaxation Level วัดเป็นระดับความผ่อนคลาย ตั้งแต่ 0 (เครียดมาก) ถึง 100 (ผ่อนคลายมากที่สุด)
# นักวิจัยต้องการใช้ Simple Linear Regression เพื่อทดสอบความสัมพันธ์นี้ จากข้อมูลไฟล์ 
# listening_time_vs_relaxation_level.csv

# สร้างชุดข้อมูล ในรูปแบบของ X (ตัวแปรอิสระ) และ Y (ตัวแปรตาม)
# ใช้ LinearRegression() จาก sklearn.linear_model เพื่อสร้างโมเดล
# Train โมเดล ด้วย model.fit(X, Y)
# คำนวณค่าคาดการณ์ ด้วย model.predict(X)
# แสดงสมการเส้นถดถอย โดยดึงค่า Slope และ Intercept
# คำนวณค่าความแม่นยำของโมเดล โดยใช้ Mean Squared Error (MSE) และ R² (R-squared)
# วาดกราฟ แสดงจุดข้อมูลจริง และเส้นสมการถดถอย